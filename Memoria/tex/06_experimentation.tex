% ============================================================
%   Chapter 6  \textemdash\  Experimentation and Results
% ============================================================

\chapter{Experimentation and Results}\label{ch:experimentation}

This chapter reports the empirical evaluation of the framework. It is organised around the four
configurations that matter: the baseline agent with no shield
(Section~\ref{sec:exp-baseline}), the basic shield (Section~\ref{sec:exp-basic}), the
adaptive-horizon shield (Section~\ref{sec:exp-adaptive}), and the effect of enabling the
curriculum (Section~\ref{sec:exp-curriculum}).
Section~\ref{sec:exp-protocol} fixes the experimental protocol,
Section~\ref{sec:exp-metrics} defines every reported metric, and
Section~\ref{sec:exp-discussion} offers a cross-configuration discussion.

\paragraph{Experimental design note.}
Due to the computational cost of training each configuration independently, the core
comparison is conducted via a \emph{cross-configuration evaluation}: a single model
(\texttt{FullSendC1\_p4}, trained with the adaptive shield on Town04) is evaluated
post-hoc under three shield configurations: \texttt{none}, \texttt{basic}, and
\texttt{adaptive}, on \(50\) seed-aligned episodes. This isolates the effect of the
shield without introducing any policy difference. A standalone adaptive-shield training run
(\texttt{learn\_baseline}) provides additional diagnostic data on the training dynamics and
curriculum progression.

\section{Experimental protocol}\label{sec:exp-protocol}

\paragraph{Hardware and software.}
All experiments are carried out on the workstation described in
Section~\ref{sec:req-constraints}:
\begin{itemize}
  \item CPU: \emph{Intel Core i9-9900K}.
  \item GPU: \emph{NVIDIA RTX 2080 Ti}.
  \item System RAM: \emph{32 GB DDR4}.
  \item Operating system: Windows 11.
  \item Python: \emph{3.12.3}.
  \item PyTorch: \emph{2.11.0} with CUDA \emph{13.0}.
  \item CARLA: 0.9.16, launched with \texttt{.\textbackslash CarlaUE4.exe -RenderOffScreen
        -carla-port=2000}.
\end{itemize}

\paragraph{Benchmark scenario.}
The main benchmark is the Town04 highway loop with clear weather (\texttt{ClearNoon}). The
vehicle spawns at a random waypoint each episode and the goal is to travel at least
\(250\,\mathrm{m}\) along the map's route graph within \(1000\) simulator steps
(\(50\,\mathrm{s}\)).

\paragraph{Training budget.}
The standalone adaptive-shield training run (\texttt{learn\_baseline}) spans
\(1\,035\) episodes with PPO updates every \(2\,048\) steps (\(\sim\!360\) gradient
updates per run). The learning rate starts at \(10^{-4}\) and anneals cosine-style to
\(10^{-5}\); the target KL for the early stop is \(0.08\); the entropy bonus starts at
\(0.001\) and decays to zero over \(50\) updates; the value-loss coefficient is \(0.50\);
all other hyperparameters take the defaults of Appendix~\ref{app:hyperparameters}.

\paragraph{Random seeds.}
Training uses \(\mathrm{seed}=42\); all evaluations use \(\mathrm{seed}=100\). Due to
computational constraints a single seed per configuration is reported; the three
independent seeds mentioned in the initial plan were not feasible in the time available.

\paragraph{Curriculum.}
Training uses a performance-based NPC curriculum with \(N_{\max} = 60\) NPCs, divided into
five stages \([0, 15, 30, 45, 60]\). Details are given in Section~\ref{sec:exp-curriculum}.

\paragraph{Evaluation protocol.}
After training, the model is evaluated on \(50\) episodes with \(\mathrm{seed}=100\) under
each shield configuration. The evaluation policy is deterministic (mean action). Models
are evaluated under the same shield configuration used during training unless the
evaluation is an explicit shield-off probe.

\section{Metrics}\label{sec:exp-metrics}

The metrics reported throughout this chapter are exactly those produced by the training and
evaluation pipelines. Their operational definitions are collected here to avoid ambiguity.

\begin{description}
  \item[\textbf{Reward/Raw\_Episode.}] Undiscounted sum of shaped rewards of a single episode.
  \item[\textbf{Reward/Average\_100\_Episodes.}] Rolling mean of \emph{Reward/Raw\_Episode} over
        the last \(100\) episodes.
  \item[\textbf{Training/Success\_Rate.}] Fraction of the last \(100\) episodes that ended with
        \texttt{info["arrive\_dest"] = True}.
  \item[\textbf{Training/Crash\_Rate.}] Fraction of the last \(100\) episodes that ended with
        \texttt{info["collision"] = True}.
  \item[\textbf{Training/Offroad\_Rate.}] Fraction of the last \(100\) episodes that ended with
        \texttt{info["out\_of\_road"] = True}.
  \item[\textbf{Safety/Shield\_Activations.}] Number of steps in which the shield overrode the
        agent during the episode.
  \item[\textbf{Safety/Shield\_Rate.}] \emph{Safety/Shield\_Activations} divided by the number of
        steps in the episode.
  \item[\textbf{Safety/Semantic/\{Dynamic,Static,Pedestrian\}\_Interventions.}] Per-episode
        intervention count by threat category, as produced by
        \texttt{CarlaAdaptiveHorizonShield.get\_statistics}.
  \item[\textbf{Safety/Semantic/\{Safe,Warning,Critical\}\_Step\_Rate.}] Fractions of steps spent
        in each risk level.
  \item[\textbf{CARLA/Mean\_Speed\_kmh.}] Arithmetic mean of \texttt{speed\_kmh} across the steps
        of the episode.
  \item[\textbf{CARLA/Speed\_Compliance\_Rate.}] Fraction of steps (with a positive speed limit)
        that satisfy \(\texttt{speed\_kmh} \le 1.05\,\texttt{speed\_limit\_kmh}\).
  \item[\textbf{Safety/Min\_\{Vehicle,Pedestrian\}\_Distance\_m.}] Minimum distance over the
        episode from the ego vehicle to the nearest vehicle / pedestrian.
  \item[\textbf{Loss/Policy\_Loss, Loss/Value\_Loss, Loss/Grad\_Norm, Training/Entropy,
        Training/Approx\_KL, Training/Epochs\_Run.}] Per-update diagnostics produced by the PPO
        loop.
\end{description}

\section{Unshielded agent\,: \texttt{shield\_type=none}}\label{sec:exp-baseline}

To establish a reference for the shield's safety benefit, the trained model
(\texttt{FullSendC1\_p4}) is evaluated on \(50\) episodes with the shield bypassed. The
policy receives the same observations but its proposed actions reach the simulator
unchanged. This probe does not correspond to a separately trained ``no-shield'' agent;
rather, it measures how well the policy performs when the safety guarantor it relied upon
during training is removed.

\paragraph{Result.}
The unshielded policy fails almost entirely on Town04 with \(20\) NPCs: it reaches the
\(250\,\mathrm{m}\) target in \emph{none} of the \(50\) episodes, exits the road on
\(84\%\) of them, and incurs a crash on the remaining \(16\%\). The mean episode reward is
\(19.8\pm18.7\) and the mean driven distance is only \(86\,\mathrm{m}\). This is the
clearest evidence of shield dependence: the adaptive-shield policy, competent with the
shield, is unsafe without it. The weaning procedure of Chapter~\ref{ch:dependence} was
developed precisely to address this failure.

\paragraph{Cross-configuration comparison.}
Figure~\ref{fig:shield-comparison} and Table~\ref{tab:config-comparison} give the full
three-way comparison (none, basic, adaptive) obtained from the same model and seed.

\begin{figure}[H]
  \centering
  \includegraphics[width=\textwidth]{imgs/shield_comparison_3way}
  \caption{Cross-configuration evaluation of \texttt{FullSendC1\_p4} (\(50\) episodes,
           \(20\) NPCs, seed \(100\)). Left: stacked outcome distribution. Centre: crash and
           off-road rates. Right: shield interventions per episode. The adaptive shield
           recovers from \(0\%\) to \(70\%\) success with only minimal crash and off-road
           residuals.}
  \label{fig:shield-comparison}
\end{figure}

\paragraph{Evaluation.}
Table~\ref{tab:baseline-eval} summarises the unshielded evaluation.

\begin{table}[H]
  \centering
  \caption{Unshielded evaluation (\texttt{shield\_type=none}), \(50\) episodes,
           \(20\) NPCs, seed \(100\).}
  \label{tab:baseline-eval}
  \small
  \begin{tabular}{@{}lc@{}}
    \toprule
    \textbf{Metric} & \textbf{Unshielded} \\
    \midrule
    Average reward        & $19.8 \pm 18.7$ \\
    Success rate          & $0\%$ \\
    Crash rate            & $16\%$ \\
    Off-road rate         & $84\%$ \\
    Stuck rate            & $0\%$ \\
    Timeout rate          & $0\%$ \\
    Mean distance (m)     & $86.3$ \\
    Shield activations/ep & $0$ \\
    \bottomrule
  \end{tabular}
\end{table}

\section{Basic shield}\label{sec:exp-basic}

The basic shield configuration evaluates the same model with
\texttt{--shield\_type basic}. The shield applies LIDAR-threshold braking and
Waypoint-API lane correction, but lacks the trajectory prediction and risk-level
classification of the adaptive variant.

\paragraph{Result.}
Adding the basic shield improves performance substantially: success rate rises from
\(0\%\) to \(12\%\), and the crash rate drops from \(16\%\) to \(4\%\). However, the
shield's conservative braking creates a new failure mode: \(42\%\) of episodes end
\emph{stuck} (the vehicle is braked to a halt and cannot self-recover). Off-road episodes
also remain high at \(42\%\), suggesting that the basic shield's heading correction is
insufficient to steer out of a developing off-road trajectory. The shield activates
\(126.2\) times per episode, roughly \(80\%\) as frequently as the adaptive shield but
with a much lower success yield.

\paragraph{Evaluation.}
Table~\ref{tab:basic-eval} reports the \(50\) evaluation episodes.

\begin{table}[H]
  \centering
  \caption{Basic shield evaluation (\texttt{shield\_type=basic}), \(50\) episodes,
           \(20\) NPCs, seed \(100\).}
  \label{tab:basic-eval}
  \small
  \begin{tabular}{@{}lc@{}}
    \toprule
    \textbf{Metric} & \textbf{Basic shield} \\
    \midrule
    Average reward           & $26.8 \pm 39.6$ \\
    Success rate             & $12\%$ \\
    Crash rate               & $4\%$ \\
    Off-road rate            & $42\%$ \\
    Stuck rate               & $42\%$ \\
    Timeout rate             & $0\%$ \\
    Mean distance (m)        & $101.7$ \\
    Shield activations / ep  & $126.2$ \\
    \bottomrule
  \end{tabular}
\end{table}

\paragraph{Intervention note.}
The basic shield does not expose the per-sub-reason breakdown (front, side, lane, heading)
in the evaluation JSON; only the total intervention count is captured. Per-reason analysis
is available via the live dashboard during interactive training sessions.

\section{Adaptive-horizon shield}\label{sec:exp-adaptive}

The adaptive-horizon shield (\texttt{--shield\_type adaptive}) is the primary configuration
studied in this work. This section reports the training dynamics of the standalone run
\texttt{learn\_baseline} (1\,035 episodes, \(60\) NPCs, adaptive shield throughout), and
the evaluation results of \texttt{FullSendC1\_p4} under the same shield.

\paragraph{Training curves.}
Figure~\ref{fig:adaptive-training} shows the rolling reward, safety rates, shield
activation rate and curriculum NPC count over the \(1\,035\) training episodes. The
\(100\)-episode rolling averages at the end of training are: reward \(293.7\), success
rate \(72.3\%\), crash rate \(2.1\%\), off-road rate \(0.0\%\). The shield is active in
\(38.7\%\) of steps; the shield-activation rate is roughly constant throughout training,
suggesting the policy is using the shield as a persistent safety layer rather than
progressively internalising its corrections.

\begin{figure}[H]
  \centering
  \includegraphics[width=\textwidth]{imgs/training_main}
  \caption{Adaptive-shield training (\texttt{learn\_baseline}, \(1\,035\) episodes,
           \(60\) NPCs). Top-left: episode reward and \(50\)-episode rolling mean.
           Top-right: \(100\)-episode rolling success, crash and off-road rates.
           Bottom-left: shield-activation rate. Bottom-right: curriculum NPC count.}
  \label{fig:adaptive-training}
\end{figure}

\paragraph{Risk distribution.}
Table~\ref{tab:adaptive-risk} reports the average time spent in each risk level over the
last \(100\) training episodes. The agent spends the majority of each episode in the
\emph{safe} regime, with roughly a quarter in \emph{warning} and under \(9\%\) in
\emph{critical}. This distribution is consistent with a policy that drives competently
in open stretches but regularly approaches the lane boundaries at speed.

\begin{table}[H]
  \centering
  \caption{Adaptive shield: average fraction of steps per risk level (last \(100\) training
           episodes, \texttt{learn\_baseline}).}
  \label{tab:adaptive-risk}
  \small
  \begin{tabular}{@{}lc@{}}
    \toprule
    \textbf{Risk level}  & \textbf{Fraction of steps} \\
    \midrule
    safe     & $65.8\%$ \\
    warning  & $25.6\%$ \\
    critical & $8.6\%$  \\
    \bottomrule
  \end{tabular}
\end{table}

\paragraph{Intervention breakdown.}
Table~\ref{tab:adaptive-breakdown} reports the per-episode intervention count by threat
category (last \(100\) training episodes). Static interventions (barriers, guardrails,
road edges) dominate by a ratio of approximately \(27{:}1\) over dynamic interventions
(vehicles). Pedestrian interventions are absent on Town04 under the traffic conditions
used. The high static rate is a structural property of the Town04 highway, since the
guardrails are continuously present at close lateral distance, rather than a sign of
aggressive driving; this is confirmed by the observation that the static rate is nearly
constant across curriculum stages while the dynamic rate rises with the NPC count.

\begin{table}[H]
  \centering
  \caption{Adaptive shield: mean interventions per episode by threat category (last
           \(100\) training episodes, \texttt{learn\_baseline}).}
  \label{tab:adaptive-breakdown}
  \small
  \begin{tabular}{@{}lc@{}}
    \toprule
    \textbf{Category}    & \textbf{Mean interventions / episode} \\
    \midrule
    dynamic    & $7.65$ \\
    static     & $202.91$ \\
    pedestrian & $0.00$ \\
    \bottomrule
  \end{tabular}
\end{table}

Figure~\ref{fig:risk-interventions} visualises both the risk-level evolution over
training and the per-category intervention breakdown.

\begin{figure}[H]
  \centering
  \includegraphics[width=\textwidth]{imgs/training_risk}
  \caption{Left: fraction of steps in each risk level (safe / warning / critical)
           over training episodes (\texttt{learn\_baseline}, 50-episode rolling mean).
           Right: mean interventions per episode by threat category over the last
           \(100\) training episodes.  Static obstacles account for \(\approx 27{:}1\)
           of all interventions versus dynamic traffic, a structural property of the
           Town04 highway geometry rather than a sign of aggressive driving.}
  \label{fig:risk-interventions}
\end{figure}

\paragraph{Evaluation.}
Table~\ref{tab:adaptive-eval} reports the \(50\) evaluation episodes of
\texttt{FullSendC1\_p4} with the adaptive shield. The policy reaches the \(250\,\mathrm{m}\)
target in \(70\%\) of episodes, incurs a crash in only \(2\%\), and is stuck
on \(14\%\). Mean speed is close to \(24\,\mathrm{km/h}\) (compliance-limited by the
\(50\,\mathrm{km/h}\) speed limit) with a speed-compliance rate of \(82.7\%$.

\begin{table}[H]
  \centering
  \caption{Adaptive shield evaluation (\texttt{FullSendC1\_p4}), \(50\) episodes,
           \(20\) NPCs, seed \(100\).}
  \label{tab:adaptive-eval}
  \small
  \begin{tabular}{@{}lc@{}}
    \toprule
    \textbf{Metric} & \textbf{Adaptive shield} \\
    \midrule
    Average reward                       & $97.8 \pm 52.5$ \\
    Success rate                         & $70\%$ \\
    Crash rate                           & $2\%$ \\
    Off-road rate                        & $12\%$ \\
    Stuck rate                           & $14\%$ \\
    Timeout rate                         & $2\%$ \\
    Mean distance (m)                    & $198.7$ \\
    Shield activations / ep              & $157.5$ \\
    Mean time in \emph{critical} (\%)    & $10.1\%$ \\
    \bottomrule
  \end{tabular}
\end{table}

\paragraph{Cross-configuration comparison.}
Table~\ref{tab:config-comparison} aggregates the evaluation of the three configurations
side by side. The adaptive shield is strictly Pareto-dominant over the basic shield on
every metric except shield interventions per episode (where it also fires more, but
achieves far higher success).

\begin{table}[H]
  \centering
  \caption{Side-by-side evaluation comparison (\texttt{FullSendC1\_p4}, \(50\) episodes,
           \(20\) NPCs, seed \(100\)).}
  \label{tab:config-comparison}
  \small
  \begin{tabular}{@{}lccc@{}}
    \toprule
    \textbf{Metric}            & \textbf{None} & \textbf{Basic} & \textbf{Adaptive} \\
    \midrule
    Average reward             & $19.8 \pm 18.7$ & $26.8 \pm 39.6$ & $97.8 \pm 52.5$ \\
    Success rate               & $0\%$           & $12\%$          & $70\%$           \\
    Crash rate                 & $16\%$          & $4\%$           & $2\%$            \\
    Off-road rate              & $84\%$          & $42\%$          & $12\%$           \\
    Stuck rate                 & $0\%$           & $42\%$          & $14\%$           \\
    Mean distance (m)          & $86.3$          & $101.7$         & $198.7$          \\
    Shield activations / ep    & $0$             & $126.2$         & $157.5$          \\
    \bottomrule
  \end{tabular}
\end{table}

\paragraph{PPO diagnostics.}
Figure~\ref{fig:ppo-diagnostics} shows the per-update diagnostics for the
\texttt{learn\_baseline} run. The \(\log\sigma\) parameters for both steering and throttle
remain well within the bounds \([\log\sigma_{\min}, \log\sigma_{\max}] = [-3.0, -1.2]\)
throughout training. The saturated-fraction metric is consistently below \(0.05\), and
the approximate KL divergence stays near the \(0.08\) target. The stable gradient norms
confirm that the tanh-squashed parameterisation successfully prevented the log-std
saturation pathology documented in the prior training phase.

\begin{figure}[H]
  \centering
  \includegraphics[width=\textwidth]{imgs/ppo_diagnostics}
  \caption{PPO update diagnostics for \texttt{learn\_baseline}. Top row: effective
           \(\log\sigma\) for steering and throttle, with the \([-3.0, -1.2]\) bounds
           (dashed). Bottom row: saturated fraction, approximate KL, entropy and gradient
           norm. The log-std values remain well within bounds throughout, and the KL is
           stable around the target.}
  \label{fig:ppo-diagnostics}
\end{figure}

\section{Effect of the curriculum}\label{sec:exp-curriculum}

The performance-based curriculum manager (Section~\ref{sec:design-curriculum}) is enabled
throughout the \texttt{learn\_baseline} run with \(N_{\max} = 60\) NPCs and five uniform
stages \([0, 15, 30, 45, 60]\). Figure~\ref{fig:curriculum-trajectory} shows the
simultaneous evolution of the NPC count and the rolling episode reward.

\begin{figure}[H]
  \centering
  \includegraphics[width=\textwidth]{imgs/curriculum_npc}
  \caption{Curriculum progression of \texttt{learn\_baseline}. Top: NPC count over
           training episodes. Bottom: \(50\)-episode rolling episode reward.
           The curriculum advances monotonically through all five stages with no rollback,
           and the rolling reward is largely maintained across each stage transition.}
  \label{fig:curriculum-trajectory}
\end{figure}

\paragraph{Stage transitions.}
Table~\ref{tab:curriculum-events} enumerates all curriculum events recorded during the
\texttt{learn\_baseline} run. The curriculum advanced cleanly through all four transitions
without triggering a single rollback, indicating that the policy satisfied the advance
thresholds (crash rate \(<\,15\%\) for stages 2--3 and \(<\,10\%\) for stage 4) within
the first \(100\) episodes at each level of difficulty.

\begin{table}[H]
  \centering
  \caption{Curriculum stage transitions during \texttt{learn\_baseline} (60 NPCs max, no
           rollbacks).}
  \label{tab:curriculum-events}
  \small
  \begin{tabular}{@{}llll@{}}
    \toprule
    \textbf{Episode} & \textbf{Event} & \textbf{From (NPCs)} & \textbf{To (NPCs)} \\
    \midrule
    100 & advance  & 0  & 15 \\
    200 & advance  & 15 & 30 \\
    300 & advance  & 30 & 45 \\
    400 & advance  & 45 & 60 \\
    \bottomrule
  \end{tabular}
\end{table}

\paragraph{Curriculum contribution.}
The clean monotonic progression with no rollbacks confirms that the curriculum
successfully matched difficulty to the current policy capability. The rolling reward is
well maintained across each \(100\)-episode stage, with no sharp drop following the
NPC-count increase. In contrast, an equivalent run at full NPC count from episode 1
would be expected to crash frequently in the early episodes and require a much longer
time to converge, a conclusion supported by the observation that the crash rate in the
first \(100\) episodes is \(<\,10\%\) precisely because the curriculum started at \(0\)
NPCs and gave the policy a period of pure lane-following to stabilise. A no-curriculum
ablation was not conducted in the available compute budget, but the anti-rollback behaviour
and rapid transitions support the expected benefit of graduated difficulty.

\section{Discussion}\label{sec:exp-discussion}

The data from Sections~\ref{sec:exp-baseline}--\ref{sec:exp-curriculum} allows each of the
four axes introduced in the chapter outline to be answered concretely.

\begin{enumerate}
  \item \textbf{Safety vs.\ performance.} The adaptive shield is strictly Pareto-better than
        the basic shield across all evaluated metrics. It achieves \(70\%\) success versus
        \(12\%\), reduces the crash rate to \(2\%\) versus \(4\%\), and nearly eliminates
        the stuck pathology (\(14\%\) versus \(42\%\)). The basic shield's conservative
        all-or-nothing braking induces a larger stuck fraction and lower mean distance than
        the adaptive variant's smooth alpha-blended projection. The reward cost of either
        shield relative to the unshielded evaluation is pronounced: from \(19.8\) to
        \(26.8\) (basic) to \(97.8\) (adaptive) average reward, demonstrating that the
        adaptive shield is the only configuration in which the agent can actually complete
        episodes.

  \item \textbf{Intervention regime.} The adaptive shield activates in \(38.7\%\) of steps
        during training (\texttt{learn\_baseline}, last \(100\) episodes), a fraction that
        is roughly constant over the training horizon. The shield-activation rate does not
        decrease as training progresses, which confirms the shield-dependence
        hypothesis of Section~\ref{sec:dep-problem}: the agent does not internalise the
        shield's corrections over the course of standard PPO training. This observation
        motivated the behavioural-cloning weaning scheme of Chapter~\ref{ch:dependence}.

  \item \textbf{Threat composition.} Static interventions (guardrail proximity) dominate
        by a \(27{:}1\) ratio over dynamic interventions in the \texttt{learn\_baseline}
        run. This ratio is a structural consequence of the Town04 highway geometry rather
        than of aggressive driving: the guardrails run continuously along both sides of the
        road at close lateral distance, and any sub-optimal lateral position triggers the
        static channel. Dynamic interventions, in contrast, rise with the NPC count and are
        concentrated in the later curriculum stages, as expected. Pedestrian interventions
        are absent on Town04.

  \item \textbf{Curriculum value.} The curriculum advances cleanly through all five stages
        with no rollback, and the rolling reward is largely maintained across each stage
        transition. The structured progression from \(0\) to \(60\) NPCs over the first
        \(400\) episodes gave the policy a stable lane-following base before the traffic
        density was increased, and the lack of rollbacks confirms that the advance thresholds
        were satisfied throughout. This supports the recommendation to enable the curriculum
        by default for any new training run on Town04.
\end{enumerate}

\paragraph{Summary.}
Table~\ref{tab:config-comparison} provides the one-stop numerical reference. The adaptive
shield is the only configuration that achieves a success rate above \(12\%\) and a crash
rate below \(5\%\) simultaneously. Its residual limitations, stuck episodes induced by
the emergency braking and the shield-dependence revealed by the unshielded probe, are
addressed in Chapter~\ref{ch:dependence}.

\clearpage
